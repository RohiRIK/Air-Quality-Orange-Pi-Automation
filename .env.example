# .env.example

# --- Application Configuration ---
# Identifier for this specific device. Will default to hostname if not set.
# DEVICE_ID=my-orange-pi-monitor

# How often the sensor reads data (in seconds).
# READ_INTERVAL=2.0

# --- Hardware Configuration (for BME680/BMP680 sensor) ---
# Forces the board type for Adafruit Blinka library.
# BLINKA_FORCEBOARD=ORANGE_PI_3_LTS

# The I2C device path. This is crucial for sensor communication.
# I2C_DEVICE=/dev/i2c-0

# --- n8n Webhook Integration ---
# URL for the n8n webhook to send sensor data for processing (e.g., AI analysis, alerts).
# Prioritizes N8N_WEBHOOK_URL_TEST if both are present.
# N8N_WEBHOOK_URL_TEST=http://your-n8n-instance/webhook-test/air-quality
# N8N_WEBHOOK_URL_PROD=http://your-n8n-instance/webhook/air-quality

# --- Google Calendar Integration (OAuth 2.0) ---
# Client ID for your Google Cloud Project (Web application credentials).
# Required for Google Calendar sync to work.
# GOOGLE_CLIENT_ID=YOUR_CLIENT_ID.apps.googleusercontent.com

# Client Secret for your Google Cloud Project.
# Required for Google Calendar sync to work.
# GOOGLE_CLIENT_SECRET=YOUR_CLIENT_SECRET

# Legacy/Backup: URL for a public iCal feed (e.g., a shared Google Calendar public link).
# GOOGLE_CALENDAR_ICS_URL=https://calendar.google.com/calendar/ical/your_calendar_id/public/basic.ics

# --- Frontend Configuration ---
# The base URL for the backend API.
# When running with Docker Compose and network_mode: host,
# Next.js rewrites handle '/api' so this can usually be left as '/api'.
# NEXT_PUBLIC_API_URL=/api
